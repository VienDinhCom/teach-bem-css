{{#is tag "input"}}
  <input class="app-input" type="{{type}}" placeholder="{{placeholder}}" />
{{/is}}

{{#is tag "textarea"}}
  <textarea class="app-input" {{#if cols}}cols="{{cols}}"{{/if}} rows="{{rows}}" placeholder="{{placeholder}}"></textarea>
{{/is}}
